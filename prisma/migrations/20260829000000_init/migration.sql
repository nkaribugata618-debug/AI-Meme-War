CREATE TYPE "GameStatus" AS ENUM ('ACTIVE', 'ENDED');

CREATE TABLE "Team" ("id" TEXT NOT NULL, "name" VARCHAR(80) NOT NULL, "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP, CONSTRAINT "Team_pkey" PRIMARY KEY ("id"));
CREATE TABLE "User" ("id" TEXT NOT NULL, "name" VARCHAR(60) NOT NULL, "teamId" TEXT, "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP, CONSTRAINT "User_pkey" PRIMARY KEY ("id"));
CREATE TABLE "Game" ("id" TEXT NOT NULL, "teamId" TEXT NOT NULL, "startsAt" TIMESTAMP(3) NOT NULL, "endsAt" TIMESTAMP(3) NOT NULL, "endedAt" TIMESTAMP(3), "status" "GameStatus" NOT NULL DEFAULT 'ACTIVE', "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP, CONSTRAINT "Game_pkey" PRIMARY KEY ("id"));
CREATE TABLE "Meme" ("id" TEXT NOT NULL, "gameId" TEXT NOT NULL, "authorId" TEXT NOT NULL, "imageUrl" TEXT NOT NULL, "prompt" VARCHAR(1000) NOT NULL, "caption" VARCHAR(500), "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP, CONSTRAINT "Meme_pkey" PRIMARY KEY ("id"));
CREATE TABLE "Vote" ("id" TEXT NOT NULL, "memeId" TEXT NOT NULL, "voterId" TEXT NOT NULL, "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP, CONSTRAINT "Vote_pkey" PRIMARY KEY ("id"));
CREATE UNIQUE INDEX "Team_name_key" ON "Team"("name");
CREATE UNIQUE INDEX "Vote_memeId_voterId_key" ON "Vote"("memeId", "voterId");
CREATE INDEX "User_teamId_idx" ON "User"("teamId");
CREATE INDEX "Game_teamId_status_idx" ON "Game"("teamId", "status");
CREATE INDEX "Game_endsAt_idx" ON "Game"("endsAt");
CREATE INDEX "Meme_gameId_idx" ON "Meme"("gameId");
CREATE INDEX "Meme_authorId_idx" ON "Meme"("authorId");
CREATE INDEX "Vote_voterId_idx" ON "Vote"("voterId");
ALTER TABLE "User" ADD CONSTRAINT "User_teamId_fkey" FOREIGN KEY ("teamId") REFERENCES "Team"("id") ON DELETE SET NULL ON UPDATE CASCADE;
ALTER TABLE "Game" ADD CONSTRAINT "Game_teamId_fkey" FOREIGN KEY ("teamId") REFERENCES "Team"("id") ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE "Meme" ADD CONSTRAINT "Meme_gameId_fkey" FOREIGN KEY ("gameId") REFERENCES "Game"("id") ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE "Meme" ADD CONSTRAINT "Meme_authorId_fkey" FOREIGN KEY ("authorId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE "Vote" ADD CONSTRAINT "Vote_memeId_fkey" FOREIGN KEY ("memeId") REFERENCES "Meme"("id") ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE "Vote" ADD CONSTRAINT "Vote_voterId_fkey" FOREIGN KEY ("voterId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE;
